/** HTTP header carrying the originating device's display name. */
export const DEVICE_NAME_HEADER = "filesync-device-name";

/** HTTP header carrying the originating device's app version. */
export const DEVICE_VERSION_HEADER = "filesync-device-version";

/** Default product name, overridable via the `VITE_APP_NAME` env var. */
export const DEFAULT_APP_NAME = "FileSync";
